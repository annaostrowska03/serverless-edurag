// Global configuration file for the Frontend
// In a deployment pipeline (e.g., GitHub Actions, Firebase deploy),
// this value should be replaced with the actual Cloud Run URL of the API.
const CONFIG = {
    API_URL: "http://localhost:8080" 
};
